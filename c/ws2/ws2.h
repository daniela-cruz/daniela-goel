#ifndef __WS2_H__
#define __WS2_H__

/* Check if the string is a palindrome */
extern int IsPalindrome(const char *str);

/* Print all numbers in a given range except for those who are related to 7 */
extern void SevenBoom(int from, int to);

/* Swap two pointers to int */
extern void Swap(int ***pp1, int ***pp2);

/* remove excess whitespace from string */
extern void RmSpaces(char *str);

/* addition of large numbers */
extern char *Adder(char *num1, char *num2, char *sum);

#endif
